version: '3'
services:
  web:
    image: nginx
    ports:
      - "8080:80"
  bdd:
    image: mysql:8
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mydb

# Lancement des services
# docker-compose up -d